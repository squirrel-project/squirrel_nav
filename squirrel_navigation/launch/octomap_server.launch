<launch>
  <node pkg="octomap_server" type="octomap_server_node" name="octomap_server">
    <!-- param name="resolution" value="0.0250" /-->
    <param name="resolution" value="0.0150" />
    <!-- fixed map frame (set to 'map' if SLAM or localization running!) -->
    <param name="frame_id" type="string" value="map" />
    <!-- maximum range to integrate (speedup, accuracy) -->
    <param name="max_sensor_range" value="4.0" />
    <param name="filter_ground" value="true" />
    <param name="plane_distance" value="0.05" />
    <param name="base_frame_id" value="/base_link" /> 
    <!-- data source to integrate (PointCloud2) -->
    <remap from="cloud_in" to="/rgbdscan" />
  </node>
</launch>
